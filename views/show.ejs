<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Fruits App</title>
    </head>
    <body>
        <h1>Fruits show page</h1>
        <a href="/fruits">Back to Index</a><br>
        The <%= fruit.name %> is <%= fruit.color %>,
        <% if(fruit.readyToEat === true) {%>
            It is ready to eat 
            <% } else {%>
            It is not ready to eat
        <% } %>         
    </body>
</html> 